services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    working_dir: /app
    command: php artisan serve --host=0.0.0.0 --port=8000
    extra_hosts:
      - "localhost:172.17.0.1"
        #      - "host.docker.internal:172.17.0.1"
